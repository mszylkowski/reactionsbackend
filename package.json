{
  "name": "pollserver",
  "version": "1.0.0",
  "description": "Server for AMP poll interactives",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mszylkowski",
  "license": "ISC",
  "dependencies": {
    "express": "^4.22.1"
  }
}
